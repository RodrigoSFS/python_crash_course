# scope, escopo = a região que a variável é reconhecida, a variável apenas fica disponível dentro da região onde foi criada
# uma versão de escopo global e local de uma variável pode ser criada 

# isso eu sá sei, as variáveis passadas como argumentos de uma função, ou dentro de uma função, são variáveis temporárias.

# o python se orienta pelo LEGB

# L = Local (Local)
# E = Enclosing (Incluindo)
# G = Global (Global)
# B = Built-in (Construídas em)